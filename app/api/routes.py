"""
API route handlers.

This module defines all REST API endpoints for the workflow engine.
Will be expanded in later phases with actual implementation.
"""

from fastapi import APIRouter

router = APIRouter()

# TODO
# - POST /graph/create
# - POST /graph/run
# - GET /graph/state/{run_id}
